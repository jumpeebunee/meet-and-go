export type IEvent = {
  id: string,
  title: string,
  cords: number[],
  place: string,
  interest: string[],
}

export type IUser = {
  username: string,
  email: string,
  uid: string,
}